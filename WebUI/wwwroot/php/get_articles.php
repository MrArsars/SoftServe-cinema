<?php
// Отримуємо категорію з параметру запиту
$category = $_GET['category'];

// Замість цього можна виконати запит до бази даних, щоб отримати необхідні статті
$articles = [
    [
        'content' => 'Вміст першої статті' . $category,
    
    ]
];

// Повертаємо дані у форматі JSON
echo json_encode($articles);

?>