// Функція для показу сторінки після затримки
function showPage() {
  document.body.style.opacity = 1; // Зміна opacity для показу контенту
}

// Встановлення затримки на 3 секунди (3000 мілісекунд)
setTimeout(showPage, 50);